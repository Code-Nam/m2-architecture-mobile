---
name: implement
description: Grants Claude permission to write under lib/ for this one turn. Use only when you deliberately want Claude to implement something instead of tutoring you through it. Usage - /implement <exact scope>.
---

<!-- AI-GENERATED by Claude (Claude Code), 2026-09-17 -->
<!-- Why: Project tooling and setup so study time goes to Flutter code. -->

The user invoked `/implement`. The `UserPromptSubmit` hook has created the
one-turn grant, so writes under `lib/` pass the guard this turn.

Rules while the grant is active:

1. Scope is exactly what follows `/implement`. Nothing else in `lib/` changes.
2. Every rule in `CLAUDE.md` still applies: architecture, naming, privacy,
   material_ui imports, no new dependencies, no generated files by hand.
3. Before writing, read the relevant pub-cache source for any API you use.
4. After writing, explain each file in chat: what it does, why it is shaped that
   way, which API it relies on, and what the user should read next.
5. Mark what you wrote: `AI-GENERATED` header on new files, BEGIN/END on
   inserted blocks, and add each file to `docs/AI_CONTRIBUTIONS.md`. Ask once
   whether a `Why:` line is wanted.
6. Run `flutter analyze`, `dart run tool/check_conventions.dart` and the
   affected tests; report the real output.
7. The grant disappears on the next prompt. Do not ask to keep it.
