# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Widget Preview related
.widget_preview/

# AI-GENERATED (Claude) BEGIN — tutor-mode entries
# Why: Project tooling and setup so study time goes to Flutter code.
# single-turn grants for lib/ writes, and per-machine Claude permissions
.claude/.implement-grant
.claude/.dartdoc-grant
.claude/settings.local.json
# AI-GENERATED (Claude) END

# AI-GENERATED (Claude) BEGIN — Firebase configuration
# Generated per project by the Firebase console / flutterfire configure.
# Delivered with the assignment submission, not committed; see README
# "Firebase configuration".
android/app/google-services.json
lib/firebase_options.dart
# AI-GENERATED (Claude) END — Firebase configuration
.tokensave

# AI-GENERATED (Claude) BEGIN — hosted yaku catalog
# Separate public repo (tenpai-api, GitHub Pages) checked out inside the
# project for convenience; tracked there, never here.
tenpai-api/
# AI-GENERATED (Claude) END — hosted yaku catalog
