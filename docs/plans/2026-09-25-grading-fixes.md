<!-- AI-GENERATED by Claude (Claude Code), 2026-09-25 -->

# Grading fixes

Goal: close the gaps found when checking the project against the grading
grid (2026-09-25). Scope chosen by the author: explain the self-hosted API
(1), move lesson logic out of the widgets (3), add the gallery (4), clear the
convention findings (5). Riverpod 2.x vs 3.x and `state.when()` are out of
scope by the author's decision.

## Tasks

0. **README + checker** — done 2026-09-25 (Claude): README explains why the
   REST API is self-hosted (no public Riichi API; endpoint checked, 200);
   `tool/check_conventions.dart` exempts notifiers that type a public
   provider (private would trip `library_private_types_in_public_api`) and
   Isar `@collection` classes (codegen needs the public name). 7 findings → 1.
1. **Verdict in the notifier** — `LessonSession` gains `bool? isCorrect`
   (null until checked) and `SenseiRequest? whyRequest`; `check(LessonBlock
   block)` computes both by switching on the block (quiz and interactive:
   `selectedOption == correctIndex`; drill: `setEquals(picked, answers)`;
   Sensei request for quiz and drill misses, drill picks sorted); `_fresh`
   resets them. `Future<bool> advance(Lesson lesson)` replaces `_continue`:
   next block, or completes the lesson and returns true so the screen pops.
   Widgets only read `session.isCorrect` / `session.whyRequest`; tile state
   mapping (`_stateOf`) stays in the widgets as presentation. Accept:
   `flutter analyze` clean, no `setEquals` / `SenseiRequest.` / `correctIndex
   ==` left under `lib/learning/widgets/`, emulator unit 0 played.
2. **Gallery** — `TilePhotoSource.capture({required bool fromGallery})`
   (`ImageSource.gallery` vs `.camera`), `scan({bool fromGallery = false})`,
   a text button « Choisir une photo » under the shutter. Accept: emulator
   scan from a gallery image.
3. **File name** — `lib/app/app.dart` → `lib/app/tenpai_app.dart` (class
   `TenpaiApp` unchanged), imports in `main.dart` and the router test.
   Accept: `check_conventions` 0 findings.
4. **Tests** — test-writer: `check()` verdicts and `whyRequest` per block
   type, `advance()` completes on the last block, gallery path through the
   fake photo source, widgets still green.
5. **Device pass + close** — emulator: a lesson, a scan from the gallery,
   airplane mode round (lesson, « Pourquoi ? », scan, Yaku Dex, sign-in,
   Profil) with no crash; ROADMAP + tasks.json with the hash.

## Deviations

- The gallery button has no handoff screen: a secondary text button under
  the shutter, in the scanner's existing style.
